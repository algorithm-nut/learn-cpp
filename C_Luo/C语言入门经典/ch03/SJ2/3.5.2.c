#include<stdio.h>
int main()
{
	char ch;
	ch='F';
	putchar(ch);
	ch='i';
	putchar(ch);
	ch='n';
	putchar(ch);
	ch='e';
	putchar(ch);
	ch='D';
	putchar(ch);
	ch='a';
	putchar(ch);
	ch='y';
	putchar(ch);
	putchar('\n');
	return 0;
}